# Go High Level inbound webhook URL (Workflow → Trigger → Inbound Webhook).
# Add this in Vercel: Project → Settings → Environment Variables → GHL_WEBHOOK_URL
GHL_WEBHOOK_URL=https://services.leadconnectorhq.com/hooks/YOUR_WEBHOOK_ID
